import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ListColector from '../../components/ListColector'

const List = () => {
  return (
    <ScrollView>
      <ListColector></ListColector>
    </ScrollView>
  )
}

export default List

const styles = StyleSheet.create({})